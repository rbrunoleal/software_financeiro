<%= form_with(model: empresa, local: true) do |form| %>
  <% if empresa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(empresa.errors.count, "error") %> prohibited this empresa from being saved:</h2>

      <ul>
      <% empresa.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :pessoajuridica_id %>
    <%= form.text_field :pessoajuridica_id %>
  </div>

  <div class="field">
    <%= form.label :endereco_id %>
    <%= form.text_field :endereco_id %>
  </div>

  <div class="field">
    <%= form.label :padrao %>
    <%= form.check_box :padrao %>
  </div>
  
  <h4>Endereço</h4>
  <div id="endereco">
    <%= form.fields_for :endereco do |endereco| %>
      <%= render 'endereco_fields', f: endereco %>
    <% end %>
    <div class="links">
      <%= link_to_add_association 'add endereco', form, :endereco %>
    </div>
  </div>
  
  <h4>Contatos</h4>
  <div id="contatos">
    <%= form.fields_for :contatoempresas do |contato| %>
      <%= render 'contatoempresa_fields', f: contato %>
    <% end %>
    <div class="links">
      <%= link_to_add_association 'add contato', form, :contatoempresas %>
    </div>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
