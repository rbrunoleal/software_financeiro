<%= form_with(model: cnao, local: true) do |form| %>
  <% if cnao.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cnao.errors.count, "error") %> prohibited this cnao from being saved:</h2>

      <ul>
      <% cnao.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :descricao %>
    <%= form.text_field :descricao %>
  </div>

  <div class="field">
    <%= form.label :codigo %>
    <%= form.text_field :codigo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
